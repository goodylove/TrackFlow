NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/trackflow
JWT_SECRET=replace-with-a-long-random-secret
CLIENT_ORIGIN=http://localhost:5173


That migration requires coordinated changes:
 ￼- Backend login sets the authentication cookie. 
 ￼- Backend middleware reads the cookie.
 ￼- Logout clears the cookie.
 ￼- Axios uses withCredentials: true.
 ￼- CORS permits credentials. ￼- 
 CSRF protection is considered based on the deployment domains.
 ￼- The token is removed from localStorage and sessionStorage ￼
- Add API documentation ￼when issue is done can someone still reassign it to another member? `


Move JWT authentication from storage to an httpOnly cookie.
Configure Axios and CORS credentials.
Implement logout and clear the cookie.
Remove stored tokens from the frontend.
Assess CSRF protection based on deployment setup.
Add API documentation. 